%%{
# ignore newline, +/- is a sign. label is allowed.

machine Lexer;

include "definitions.csrl";

EXPR_BEG_LABEL := |*

*|;

}%%
namespace Mint.Lex.States
{
    internal partial class BegLabel
    {
        public override State Advance()
        {
            var ts = -1;
			var te = -1;
			var act = 0;
			var cs = Lexer_start;
			var pe = Lexer.Length;
			var eof = pe;

            %% write exec;

            return null;
        }

        %% write data;
    }
}